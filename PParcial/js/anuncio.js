export default class Anuncio {
    constructor(id, titulo, precio, descripcion){
        this.id = id;
        this.titulo = titulo;
        this.precio = precio;
        this.descripcion = descripcion;
    }
}